<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_body"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="fill_vertical"
    android:orientation="vertical"
    android:padding="14dp"
    tools:layout_width="110dp"
    tools:layout_height="110dp">

    <TextView
        android:id="@+id/runway_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autoSizeMaxTextSize="24sp"
        android:autoSizeMinTextSize="14sp"
        android:autoSizeStepGranularity="2sp"
        android:autoSizeTextType="uniform"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:text="1m 29d"
        android:textColor="@color/runway_ink"
        android:textSize="20sp"
        android:fontWeight="300"  />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:letterSpacing="0.05"
        android:text="PERSONAL RUNWAY"
        android:textColor="@color/runway_muted"
        android:textSize="10sp" />

    <TextView
        android:layout_width="1dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <ImageView
        android:id="@+id/liquid_runway_image"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:contentDescription="Liquid runway"
        android:scaleType="fitStart" />
</LinearLayout>
